import React from 'react'
import styled, { css } from 'styled-components';

function SeatChangePage({}){
    return (
        <S.SeatChangePage>
            seat change
        </S.SeatChangePage>
    )
}

export default SeatChangePage;

const S = {
    SeatChangePage: styled.div`
    `
};